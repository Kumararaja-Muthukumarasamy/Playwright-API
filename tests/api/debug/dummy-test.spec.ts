import { test, expect } from '@playwright/test';

test('dummy test for PR validation', async () => {
  expect(1).toBe(1);
});

test('dummy test for PR validation 1', async () => {
  expect(1).toBe(1);
});